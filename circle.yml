machine:
  services:
    - docker

dependencies:
  override:
    - ./dependencies.sh

database:
  override:
    - echo "Don't need to run any databse setup."

test:
  pre:
    - mv env.test .env
  override:
    - ./test.sh

deployment:
  production:
    branch: master
    commands:
      - ./deploy.sh
